"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getArtifactsClient = getArtifactsClient;
const databricks_1 = require("./databricks");
const mlflow_1 = require("./mlflow");
/**
 * Get the appropriate artifacts client based on the tracking URI.
 *
 * @param trackingUri - The tracking URI to use to determine the artifacts client.
 * @returns The appropriate artifacts client.
 */
function getArtifactsClient({ trackingUri, host, databricksToken }) {
    if (trackingUri.startsWith('databricks')) {
        return new databricks_1.DatabricksArtifactsClient({ host, databricksToken });
    }
    else {
        return new mlflow_1.MlflowArtifactsClient({ host });
    }
}
